# HONEST AI USAGE LOG

This document tracks how AI was used, specifically focusing on course corrections, architectural steering, and debugging AI hallucinations.

### Date: [Isi Tanggal]
* **Tool Used:** [Cursor / Claude 3.5 Sonnet]
* **Context / Task:** Implementing the `tokio::sync::mpsc` channel for the MQTT consumer.
* **Where the AI was wrong:** The AI initially suggested blocking the async runtime by executing `sqlx::query!` directly inside the MQTT callback loop. This would have caused severe bottlenecks during the Network Burst test case.
* **How I corrected it:** I steered the AI to implement an `mpsc` channel instead. The MQTT listener now only pushes to the channel (non-blocking), and a separate dedicated worker task consumes the channel and handles the database batch inserts.

### Date: ...
* **Tool Used:** ...
* **Context / Task:** ...
* **Where the AI was wrong:** ...
* **How I corrected it:** ...